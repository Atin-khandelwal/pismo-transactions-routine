CREATE TABLE IF NOT EXISTS Operations (
id INTEGER AUTO_INCREMENT,
description VARCHAR(255) NOT NULL,
evaluation_type ENUM('POSITIVE','NEGATIVE') NOT NULL
);

CREATE TABLE IF NOT EXISTS accounts (
id INTEGER AUTO_INCREMENT,
ACCOUNT_NUMBER INTEGER NOT NULL,
ACCOUNT_HOLDER_NAME VARCHAR(250) NOT NULL,
CREATED_AT TIMESTAMP NOT NULL,
ACCOUNT_TYPE VARCHAR(50)
);

CREATE TABLE IF NOT EXISTS transactions (
id INTEGER AUTO_INCREMENT,
ACCOUNT_ID INTEGER NOT NULL,
OPERATION_ID INTEGER NOT NULL,
EVENT_DATE TIMESTAMP NOT NULL,
AMOUNT DECIMAL NOT NULL
);

INSERT INTO Operations (description,evaluation_type)
VALUES('Normal Purchase','NEGATIVE'),
('Purchase with installments','NEGATIVE'),
('Withdrawal','NEGATIVE'),
('Credit Voucher','POSITIVE');
